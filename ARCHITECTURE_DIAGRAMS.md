# 项目架构可视化 Architecture Visualization

## 当前架构图 Current Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                     Yuzuriha Rin Project                        │
│                    Virtual Character System                     │
└─────────────────────────────────────────────────────────────────┘
                                 │
                    ┌────────────┴────────────┐
                    │                         │
            ┌───────▼────────┐        ┌──────▼──────┐
            │   Frontend     │        │   Backend   │
            │  (Browser UI)  │◄──WS───┤  (FastAPI)  │
            └───────┬────────┘        └──────┬──────┘
                    │                        │
                    │                        │
┌───────────────────┴────────────────────────┴───────────────────┐
│                    File Organization                           │
└────────────────────────────────────────────────────────────────┘

项目根目录 Project Root
│
├── 🔴 frontend/                    ⚠️ 应该在 src/ 内
│   ├── scripts/                    Should be in src/
│   │   ├── core/                   (API, WebSocket, State)
│   │   ├── ui/                     (UI Components)
│   │   ├── utils/                  (Utilities)
│   │   └── views/                  (Chat, Session List)
│   ├── styles/                     (CSS)
│   └── index.html
│
├── ✅ src/                          核心应用代码 Core App
│   ├── api/                        API 层 (FastAPI routes)
│   │   ├── main.py                 (App entry, middleware)
│   │   ├── routes.py               (REST API)
│   │   ├── ws_routes.py            (WebSocket per session)
│   │   └── ws_global_routes.py     (WebSocket global)
│   │
│   ├── core/                       核心领域 Core Domain
│   │   ├── config/
│   │   │   └── settings.py         (All configurations)
│   │   └── models/                 (Domain models)
│   │       ├── character.py        (Character entity)
│   │       ├── message.py          (Message entity)
│   │       ├── session.py          (Session entity)
│   │       └── behavior.py         (Behavior value objects)
│   │
│   ├── infrastructure/             基础设施 Infrastructure
│   │   ├── database/
│   │   │   ├── connection.py       (SQLite connection)
│   │   │   └── repositories/       (Data access)
│   │   │       ├── base.py         (Base repository)
│   │   │       ├── character_repo.py
│   │   │       ├── message_repo.py
│   │   │       └── session_repo.py
│   │   ├── network/
│   │   │   ├── websocket_manager.py (WS management)
│   │   │   └── port_manager.py      (Port allocation)
│   │   └── utils/
│   │       └── logger.py            (Logging)
│   │
│   ├── services/                   应用服务 Application Services
│   │   ├── behavior/               (Behavior orchestration)
│   │   │   ├── coordinator.py      (Main coordinator)
│   │   │   ├── emotion.py          (Emotion system)
│   │   │   ├── pause.py            (Pause simulation)
│   │   │   ├── segmenter.py        (Text segmentation)
│   │   │   ├── sticker.py          (Sticker selection)
│   │   │   ├── timeline.py         (Typing timeline)
│   │   │   └── typo.py             (Typo injection)
│   │   ├── character/              (Character management)
│   │   ├── config/                 (Config management)
│   │   ├── llm/                    (LLM client)
│   │   ├── messaging/              (Message service)
│   │   ├── session/                (Session client)
│   │   └── tools/                  (Tool service)
│   │
│   └── utils/                      通用工具 Utilities
│       ├── image_alter.py          (Image processing)
│       └── url_utils.py            (URL utilities)
│
├── 🔴 models/                       ⚠️ 应改名为 scripts/ml_training/
│   └── scripts/                    Should rename to scripts/ml_training/
│       ├── train_wechat_v2.py      (Model training)
│       ├── predict_windows.py      (Prediction)
│       └── ...
│
├── ✅ tools/                        独立工具 Standalone Tools
│   └── sticker_manager/            (PyQt6 GUI tool)
│       ├── sticker_manager.py      (Main app)
│       └── sticker_categories.py   (Categories)
│
├── 🟡 data/                         ⚠️ 需要重组
│   ├── stickers/                   → 应移到 assets/
│   ├── jieba/                      → 应移到 assets/
│   ├── raw/                        → 应移到 archive/
│   ├── image_alter.json            → 应移到 assets/config/
│   └── rin_app.db                  → 应移到 data/database/
│
├── ✅ tests/                        测试 Tests
│   ├── test_character_update.py
│   ├── test_message_recall.py
│   └── ...
│
├── run.py                          应用入口 App Entry
└── pyproject.toml                  项目配置 Project Config

图例 Legend:
✅ = 良好 Good
🟡 = 需改进 Needs Improvement  
🔴 = 应该修改 Should Change
```

---

## 分层架构图 Layered Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                      Client Layer                           │
│                    (Browser / WebView)                      │
│                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│  │  Chat View   │  │ Session List │  │  UI Widgets  │    │
│  └──────────────┘  └──────────────┘  └──────────────┘    │
└──────────────────────────┬──────────────────────────────────┘
                           │ WebSocket / HTTP
┌──────────────────────────▼──────────────────────────────────┐
│                       API Layer                             │
│                   (src/api/)                                │
│                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│  │  REST Routes │  │  WS Routes   │  │  WS Global   │    │
│  └──────────────┘  └──────────────┘  └──────────────┘    │
└──────────────────────────┬──────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────┐
│                    Service Layer                            │
│                  (src/services/)                            │
│                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│  │   Session    │  │   Behavior   │  │     LLM      │    │
│  │   Client     │  │ Coordinator  │  │    Client    │    │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘    │
│         │                  │                  │            │
│  ┌──────▼───────┐  ┌──────▼───────┐  ┌──────▼───────┐    │
│  │   Message    │  │   Character  │  │     Tool     │    │
│  │   Service    │  │   Service    │  │   Service    │    │
│  └──────────────┘  └──────────────┘  └──────────────┘    │
└──────────────────────────┬──────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────┐
│                     Domain Layer                            │
│                  (src/core/models/)                         │
│                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│  │   Character  │  │    Message   │  │   Session    │    │
│  └──────────────┘  └──────────────┘  └──────────────┘    │
│                                                             │
│  ┌──────────────┐  ┌──────────────┐                       │
│  │   Behavior   │  │   Constants  │                       │
│  └──────────────┘  └──────────────┘                       │
└──────────────────────────┬──────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────┐
│                Infrastructure Layer                         │
│               (src/infrastructure/)                         │
│                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│  │   Database   │  │   WebSocket  │  │    Logger    │    │
│  │  Connection  │  │   Manager    │  │              │    │
│  └──────┬───────┘  └──────────────┘  └──────────────┘    │
│         │                                                  │
│  ┌──────▼───────────────────────────────────┐            │
│  │          Repositories                    │            │
│  │  ┌────────┐ ┌────────┐ ┌────────┐       │            │
│  │  │ Char   │ │  Msg   │ │Session │       │            │
│  │  └────────┘ └────────┘ └────────┘       │            │
│  └──────────────────────────────────────────┘            │
└──────────────────────────┬──────────────────────────────────┘
                           │
                  ┌────────▼────────┐
                  │   SQLite DB     │
                  │  (data/*.db)    │
                  └─────────────────┘
```

---

## 数据流图 Data Flow

```
用户发送消息 User sends message
         │
         ▼
┌─────────────────┐
│  Frontend View  │ chatView.js
└────────┬────────┘
         │ WebSocket.send()
         ▼
┌─────────────────┐
│  WS Manager     │ websocket_manager.py
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Session Client  │ session_client.py
└────────┬────────┘
         │
    ┌────┴────┐
    │         │
    ▼         ▼
┌─────────┐ ┌──────────────┐
│ Message │ │   Behavior   │
│ Service │ │ Coordinator  │
└────┬────┘ └──────┬───────┘
     │             │
     │      ┌──────┴──────┐
     │      │             │
     │      ▼             ▼
     │  ┌────────┐   ┌─────────┐
     │  │Timeline│   │ Emotion │
     │  └────────┘   └─────────┘
     │      │             │
     │      ▼             ▼
     │  ┌────────┐   ┌─────────┐
     │  │ Typo   │   │ Sticker │
     │  └────────┘   └─────────┘
     │             │
     ▼             ▼
┌─────────────────────┐
│    LLM Client       │ llm_client.py
└──────────┬──────────┘
           │
           ▼
    ┌──────────────┐
    │  DeepSeek AI │ (External)
    └──────┬───────┘
           │ Response
           ▼
┌─────────────────────┐
│ Behavior Processing │
│  (Typo, Pause, etc) │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│   Message Repo      │
│   (Save to DB)      │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│   WS Broadcast      │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│   Frontend Update   │
│  (Render messages)  │
└─────────────────────┘
```

---

## 推荐的目录结构图 Recommended Structure

```
Yuzuriha-Rin/
│
├── 📦 src/                         所有源代码 ALL SOURCE CODE
│   │
│   ├── 🌐 frontend/                ★ 从根目录移入 MOVED
│   │   ├── scripts/
│   │   │   ├── core/               (WebSocket, State, API)
│   │   │   ├── ui/                 (UI Components)
│   │   │   ├── utils/              (Helper functions)
│   │   │   └── views/              (Chat, Session List)
│   │   ├── styles/
│   │   ├── images/
│   │   └── index.html
│   │
│   ├── 🔌 api/                     API 接口层
│   │   ├── main.py                 (FastAPI app)
│   │   ├── routes.py               (HTTP routes)
│   │   ├── ws_routes.py            (WebSocket)
│   │   └── schemas.py              (Pydantic schemas)
│   │
│   ├── 💎 core/                    核心领域
│   │   ├── config/
│   │   │   └── settings.py         (Configuration)
│   │   └── models/
│   │       ├── character.py        (Character entity)
│   │       ├── message.py          (Message entity)
│   │       ├── session.py          (Session entity)
│   │       └── behavior.py         (Behavior models)
│   │
│   ├── 🏗️ infrastructure/          基础设施
│   │   ├── database/
│   │   │   ├── connection.py
│   │   │   └── repositories/
│   │   ├── network/
│   │   │   ├── websocket_manager.py
│   │   │   └── port_manager.py
│   │   └── utils/
│   │       └── logger.py
│   │
│   ├── ⚙️ services/                应用服务
│   │   ├── behavior/
│   │   │   ├── coordinator.py
│   │   │   ├── emotion.py
│   │   │   ├── timeline.py
│   │   │   └── ...
│   │   ├── character/
│   │   ├── llm/
│   │   ├── messaging/
│   │   └── session/
│   │
│   └── 🛠️ utils/                   通用工具
│       ├── image_alter.py
│       └── url_utils.py
│
├── 🧪 tests/                       测试代码
│   ├── test_*.py
│   └── ...
│
├── 📜 scripts/                     ★ 从 models/ 重命名
│   └── ml_training/                ML 训练脚本
│       ├── train_wechat_v2.py
│       └── ...
│
├── 🔧 tools/                       独立工具
│   └── sticker_manager/            (PyQt6 GUI)
│
├── 💾 data/                        ★ 仅运行时数据
│   └── database/
│       └── rin_app.db
│
├── 🎨 assets/                      ★ 新增：只读资源
│   ├── stickers/                   (从 data/ 移入)
│   ├── jieba/                      (从 data/ 移入)
│   └── config/
│       └── image_alter.json        (从 data/ 移入)
│
├── 📚 archive/                     ★ 新增：归档资源
│   └── raw/                        (从 data/raw 移入)
│       ├── avatar/
│       └── stickers/
│
├── 📋 logs/                        (可选) 应用日志
├── 📖 docs/                        (可选) 文档
│
├── 🚀 run.py                       应用入口
├── ⚙️ pyproject.toml               项目配置
├── 🧪 pytest.ini                   测试配置
├── 📄 README.md
└── 📊 ARCHITECTURE_ANALYSIS.md     (本分析报告)

图例 Legend:
★ = 需要改动 Changes Required
📦 = 源代码 Source Code
🌐 = 前端 Frontend
🔌 = API接口 API
💎 = 核心领域 Core Domain
🏗️ = 基础设施 Infrastructure
⚙️ = 服务 Services
🧪 = 测试 Tests
📜 = 脚本 Scripts
🔧 = 工具 Tools
💾 = 数据 Data
🎨 = 资源 Assets
📚 = 归档 Archive
```

---

## 依赖关系图 Dependency Graph

```
                    ┌──────────────┐
                    │   Frontend   │
                    │ (JavaScript) │
                    └───────┬──────┘
                            │
                    ┌───────▼──────┐
                    │   API Layer  │
                    │  (FastAPI)   │
                    └───────┬──────┘
                            │
                    ┌───────▼──────────┐
                    │  Service Layer   │
                    └───────┬──────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
┌───────▼──────┐    ┌───────▼──────┐   ┌───────▼──────┐
│ Domain Layer │    │Infrastructure│   │  External    │
│   (Models)   │    │    Layer     │   │   Services   │
└──────────────┘    └───────┬──────┘   └──────────────┘
                            │
                    ┌───────▼──────┐
                    │   Database   │
                    │   (SQLite)   │
                    └──────────────┘

依赖规则 Dependency Rules:
→ 允许依赖 Allowed
✗ 禁止依赖 Forbidden

API Layer → Service Layer ✓
Service Layer → Domain Layer ✓
Service Layer → Infrastructure Layer ✓
Infrastructure Layer → Domain Layer ✓

Domain Layer → Service Layer ✗
Domain Layer → Infrastructure Layer ✗
Infrastructure Layer → Service Layer ✗
```

---

## 模块职责 Module Responsibilities

```
┌─────────────────────────────────────────────────────────┐
│ frontend/                                               │
│ 职责: 用户界面和交互                                    │
│ Responsibility: User interface and interaction          │
│ - WebSocket 连接管理                                    │
│ - 状态管理 (messages, sessions, UI state)              │
│ - 消息渲染和动画                                        │
│ - 用户输入处理                                          │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ src/api/                                                │
│ 职责: HTTP/WebSocket 接口                               │
│ Responsibility: HTTP/WebSocket endpoints                │
│ - 路由定义                                              │
│ - 请求验证                                              │
│ - WebSocket 连接管理                                    │
│ - 静态文件服务                                          │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ src/services/                                           │
│ 职责: 业务逻辑编排                                      │
│ Responsibility: Business logic orchestration            │
│ - SessionClient: 会话管理和消息处理                     │
│ - BehaviorCoordinator: 行为编排                         │
│ - MessageService: 消息CRUD                              │
│ - CharacterService: 角色管理                            │
│ - LLMClient: AI 通信                                    │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ src/core/models/                                        │
│ 职责: 领域模型定义                                      │
│ Responsibility: Domain model definitions                │
│ - Character: 角色实体                                   │
│ - Message: 消息实体                                     │
│ - Session: 会话实体                                     │
│ - Behavior: 行为值对象                                  │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ src/infrastructure/                                     │
│ 职责: 技术基础设施                                      │
│ Responsibility: Technical infrastructure                │
│ - Database: 数据持久化                                  │
│ - Repositories: 数据访问                                │
│ - WebSocketManager: WS 连接池                           │
│ - Logger: 日志系统                                      │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ models/ (应改为 scripts/ml_training/)                  │
│ 职责: 机器学习模型训练                                  │
│ Responsibility: ML model training                       │
│ - 意图识别模型训练                                      │
│ - 模型预测脚本                                          │
│ - 开发和离线使用                                        │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ tools/                                                  │
│ 职责: 独立开发工具                                      │
│ Responsibility: Standalone development tools            │
│ - StickerManager: 表情包管理 GUI                        │
│ - 独立运行，不依赖主应用                                │
└─────────────────────────────────────────────────────────┘
```

---

**生成日期 Generated:** 2025-12-15  
**项目版本 Project Version:** 0.1.0
